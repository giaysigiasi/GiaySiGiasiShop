@model ProductDetailsModel
@using Nop.Web.Models.Catalog;
@if (Model.EmailAFriendEnabled)
{
    <li>
        <div class="email-a-friend">
            <a href="javascript:void(0);" onclick="setLocation('@Url.RouteUrl("ProductEmailAFriend", new { productId = Model.Id })')">
                <span class="share-icon"></span>
                @T("Products.EmailAFriend")
            </a>
            @*<input type="button" value="@T("Products.EmailAFriend")" class="button-2 email-a-friend-button" onclick="setLocation('@Url.RouteUrl("ProductEmailAFriend", new { productId = Model.Id })')" />*@
        </div>
    </li>
}
