@model IList<CategoryModel>

@using Nop.Web.Models.Catalog;
@if (Model.Count > 0)
{
    @*<div class="category-grid home-page-category-grid">
            <div class="item-grid">
                @foreach (var item in Model)
                {
                    <div class="item-box">
                        <div class="category-item">
                            <h2 class="title">
                                <a href="@Url.RouteUrl("Category", new { SeName = item.SeName })" title="@item.PictureModel.Title">
                                    @item.Name
                                </a>
                            </h2>
                            <div class="picture">
                                <a href="@Url.RouteUrl("Category", new { SeName = item.SeName })" title="@item.PictureModel.Title">
                                    <img alt="@item.PictureModel.AlternateText" src="@item.PictureModel.ImageUrl" title="@item.PictureModel.Title" />
                                </a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>*@


    <div class="home-product-section">
        <div class="product-container">
            <div class="row no-margin">
                @foreach (var item in Model)
                {
                    <div class="col-sm-6 no-padding">
                        <div class="relative-box"><a href="@Url.RouteUrl("Category", new { SeName = item.SeName })"> <img src="@item.PictureModel.ImageUrl" alt="@item.PictureModel.AlternateText" /> <span class="hover-text-box">@item.Name <span class="white-line">.</span><span class="sub-text">@Html.Raw(item.Description)</span></span> <span class="image-overlay">.</span></a></div>
                    </div>
                }
            </div>
        </div>
    </div>
}
